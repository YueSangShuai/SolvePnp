#include "Thread/MyThread.h"
#include "AutoShoot/Inference/TRT/TRTModule.h"
#include "AutoShoot/Pnpsolver/PnpSolver.h"
using namespace std;

int main() {
//    MyThread mythread;
//    thread *ImageProducer=new thread(std::bind(&MyThread::ImageProducter,&mythread));
//    thread *ImageConsumer=new thread(std::bind(&MyThread::ImageConsumer,&mythread));
//    thread *ImagePlot=new thread(std::bind(&MyThread::showPlot,&mythread));
//
//    ImageProducer->join();
//    ImageConsumer->join();
//    ImagePlot->join();
PNPSolver pnpSolver;

}
